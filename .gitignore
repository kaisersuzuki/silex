# Datasets (fetched from storage.googleapis.com/cvdf-datasets/mnist/)
data/

# All build/ contents are regenerated by: make train train-cnn verify verify-cnn fpga
build/

# Simulation waveforms
*.vcd

# OpenLane: runs and logs are regenerated by `make asic`; gen/ is derived RTL
asic/runs/
asic/gen/
asic/flow.log
.venv-openlane/
